# monogate.core

Core EML operator and elementary function identities.

No external dependencies — uses only `math.exp` and `math.log`.

::: monogate.core
    options:
      show_source: false
      members:
        - op
        - E
        - ZERO
        - NEG_ONE
        - exp_eml
        - ln_eml
        - add_eml
        - sub_eml
        - mul_eml
        - div_eml
        - pow_eml
        - neg_eml
        - recip_eml
