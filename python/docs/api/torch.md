# monogate.torch

Differentiable EML layers for PyTorch. Drop-in activations and learnable expression layers.

Requires `torch >= 2.0`.

::: monogate.torch.EMLActivation

::: monogate.torch.EMLLayer

::: monogate.torch.compare_to_native
