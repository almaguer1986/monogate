# monogate.network

PyTorch neural network classes for differentiable EML arithmetic.

Requires `torch >= 2.0`.

::: monogate.network
    options:
      show_source: false
      members:
        - EMLNetwork
        - HybridNetwork
        - EMLTree
        - fit
