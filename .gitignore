node_modules/
dist/
*.log
.DS_Store
.claude/

# Lean formalization — private research skeleton, not published
lean/

# Private BEST routing config — source and compiled binary
# Regenerate binary with: python scripts/compile_routing.py
python/monogate/_routing_private.py
python/monogate/_routing.pkl

# Private research files — kept in monogate-research (private repo)
CONTEXT.md
RESEARCH_IDEAS.md
internal/
sessions/raw/
*.private.md
research_notes/
python/experiments/
python/paper/arxiv_draft_*.md
python/paper/weierstrass_proof_*.tex
python/paper/arxiv_submission_notes.md

# Private repo, sometimes checked out as a nested directory for local
# convenience. Must never be tracked inside this public monorepo.
monogate-research/

# Canonical monogate-dev lives at its own repo (github.com/agent-maestro/monogate-dev)
# and on disk at D:/monogate-dev/. Never nest it inside this monorepo — the
# duplicate checkout drifted out of sync and was cleaned up.
monogate-dev/
