import MonogateEML.DivLowerBound3

open Real

namespace DivLowerBound3Full

private lemma exp2_gt_7 : (7 : ℝ) < Real.exp 2 := by
  have h := Real.exp_add 1 1
  nlinarith [Real.exp_one_gt_d9, Real.exp_pos 1]

private lemma exp3_gt_19 : (19 : ℝ) < Real.exp 3 := by
  have h12 := Real.exp_add 1 2
  have h2 := exp2_gt_7
  nlinarith [Real.exp_one_gt_d9]

private lemma exp6_gt_390 : (390 : ℝ) < Real.exp 6 := by
  have h33 := Real.exp_add 3 3
  nlinarith [exp3_gt_19]

private lemma log3_gt_1 : (1 : ℝ) < Real.log 3 := by
  have : Real.log (Real.exp 1) < Real.log 3 :=
    Real.log_lt_log (Real.exp_pos 1) (by linarith [Real.exp_one_lt_d9])
  rwa [Real.log_exp] at this

private lemma log6_gt_1 : (1 : ℝ) < Real.log 6 := by
  have : Real.log (Real.exp 1) < Real.log 6 :=
    Real.log_lt_log (Real.exp_pos 1) (by linarith [Real.exp_one_lt_d9])
  rwa [Real.log_exp] at this

private lemma log3_lt_2 : Real.log 3 < (2 : ℝ) := by
  have : Real.log 3 < Real.log (Real.exp 2) :=
    Real.log_lt_log (by norm_num) (by linarith [exp2_gt_7])
  rwa [Real.log_exp] at this

private lemma log6_lt_2 : Real.log 6 < (2 : ℝ) := by
  have : Real.log 6 < Real.log (Real.exp 2) :=
    Real.log_lt_log (by norm_num) (by linarith [exp2_gt_7])
  rwa [Real.log_exp] at this

private lemma circ_0001_F1_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0002_F1_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0003_F1_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0004_F1_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0005_F1_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0006_F1_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0007_F1_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0008_F1_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0009_F1_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0010_F1_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0011_F1_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0012_F1_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0013_F1_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0014_F1_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0015_F1_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0016_F1_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0017_F1_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0018_F1_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0019_F1_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0020_F1_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0021_F1_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0022_F1_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0023_F1_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0024_F1_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0025_F1_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0026_F1_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0027_F1_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0028_F1_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0029_F1_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0030_F1_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0031_F1_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0032_F1_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0033_F1_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log6_gt_1]

private lemma circ_0034_F1_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0035_F1_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log3_gt_1]

private lemma circ_0036_F1_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0037_F1_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log6_gt_1]

private lemma circ_0038_F1_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0039_F1_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log3_gt_1]

private lemma circ_0040_F1_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0041_F1_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log6_gt_1]

private lemma circ_0042_F1_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0043_F1_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log3_gt_1]

private lemma circ_0044_F1_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0045_F1_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log6_gt_1]

private lemma circ_0046_F1_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0047_F1_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log3_gt_1]

private lemma circ_0048_F1_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0049_F1_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log6_gt_1]

private lemma circ_0050_F1_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0051_F1_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log3_gt_1]

private lemma circ_0052_F1_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0053_F1_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log6_gt_1]

private lemma circ_0054_F1_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0055_F1_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log3_gt_1]

private lemma circ_0056_F1_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0057_F1_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log6_gt_1]

private lemma circ_0058_F1_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0059_F1_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log3_gt_1]

private lemma circ_0060_F1_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0061_F1_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log6_gt_1]

private lemma circ_0062_F1_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0063_F1_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log3_gt_1]

private lemma circ_0064_F1_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0065_F1_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0066_F1_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0067_F1_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0068_F1_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0069_F1_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0070_F1_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0071_F1_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0072_F1_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0073_F1_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0074_F1_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0075_F1_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0076_F1_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0077_F1_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0078_F1_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0079_F1_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0080_F1_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0081_F1_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log6_gt_1]

private lemma circ_0082_F1_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0083_F1_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log3_gt_1]

private lemma circ_0084_F1_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0085_F1_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log6_gt_1]

private lemma circ_0086_F1_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0087_F1_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log3_gt_1]

private lemma circ_0088_F1_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0089_F1_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log6_gt_1]

private lemma circ_0090_F1_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0091_F1_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log3_gt_1]

private lemma circ_0092_F1_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0093_F1_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log6_gt_1]

private lemma circ_0094_F1_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0095_F1_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log3_gt_1]

private lemma circ_0096_F1_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0097_F1_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0098_F1_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0099_F1_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0100_F1_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0101_F1_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0102_F1_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0103_F1_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0104_F1_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0105_F1_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0106_F1_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0107_F1_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0108_F1_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0109_F1_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0110_F1_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0111_F1_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0112_F1_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0113_F1_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log6_gt_1]

private lemma circ_0114_F1_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0115_F1_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log3_gt_1]

private lemma circ_0116_F1_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0117_F1_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log6_gt_1]

private lemma circ_0118_F1_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0119_F1_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log3_gt_1]

private lemma circ_0120_F1_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0121_F1_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log6_gt_1]

private lemma circ_0122_F1_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0123_F1_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log3_gt_1]

private lemma circ_0124_F1_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0125_F1_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log6_gt_1]

private lemma circ_0126_F1_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0127_F1_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log3_gt_1]

private lemma circ_0128_F1_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0129_F1_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0130_F1_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0131_F1_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0132_F1_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0133_F1_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0134_F1_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0135_F1_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0136_F1_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0137_F1_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F1: exp(v) - log(6) < 2. v = 1.2082, exp(v) < 2+log(6).
  have he3 : Real.exp 3 = Real.exp 1 * Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add, ← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0138_F1_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0139_F1_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F1: exp(v) - log(3) > 2. v = 1.2082, exp(v) > 2+log(3).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0140_F1_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0141_F1_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0142_F1_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0143_F1_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0144_F1_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0145_F1_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0146_F1_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0147_F1_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0148_F1_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0149_F1_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0150_F1_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0151_F1_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0152_F1_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0153_F1_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F1: exp(v) - log(6) < 2. v = 1.2082, exp(v) < 2+log(6).
  have he3 : Real.exp 3 = Real.exp 1 * Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add, ← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0154_F1_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_
_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0243_F1_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0244_F1_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0245_F1_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0246_F1_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0247_F1_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0248_F1_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0249_F1_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0250_F1_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0251_F1_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0252_F1_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0253_F1_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0254_F1_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F1 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0255_F1_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F1 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0256_F1_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F1 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F1, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0257_F2_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0258_F2_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0259_F2_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0260_F2_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0261_F2_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0262_F2_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0263_F2_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0264_F2_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0265_F2_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0266_F2_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0267_F2_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0268_F2_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0269_F2_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0270_F2_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0271_F2_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0272_F2_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0273_F2_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0274_F2_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0275_F2_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0276_F2_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0277_F2_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0278_F2_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0279_F2_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0280_F2_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0281_F2_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0282_F2_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0283_F2_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0284_F2_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0285_F2_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0286_F2_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0287_F2_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0288_F2_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0289_F2_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log6_gt_1]

private lemma circ_0290_F2_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0291_F2_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0292_F2_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0293_F2_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log6_gt_1]

private lemma circ_0294_F2_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0295_F2_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0296_F2_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0297_F2_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log6_gt_1]

private lemma circ_0298_F2_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0299_F2_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0300_F2_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0301_F2_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log6_gt_1]

private lemma circ_0302_F2_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0303_F2_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0304_F2_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0305_F2_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log6_gt_1]

private lemma circ_0306_F2_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0307_F2_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0308_F2_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0309_F2_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log6_gt_1]

private lemma circ_0310_F2_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0311_F2_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0312_F2_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0313_F2_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log6_gt_1]

private lemma circ_0314_F2_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0315_F2_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0316_F2_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0317_F2_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.
y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0477_F2_F14_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F14 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0478_F2_F14_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0479_F2_F14_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F14 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0480_F2_F14_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0481_F2_F15_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0482_F2_F15_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0483_F2_F15_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0484_F2_F15_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0485_F2_F15_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0486_F2_F15_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0487_F2_F15_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0488_F2_F15_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0489_F2_F15_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0490_F2_F15_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0491_F2_F15_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0492_F2_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0493_F2_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0494_F2_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0495_F2_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0496_F2_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0497_F2_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0498_F2_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0499_F2_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0500_F2_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0501_F2_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0502_F2_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0503_F2_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0504_F2_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0505_F2_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0506_F2_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0507_F2_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0508_F2_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0509_F2_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0510_F2_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F2 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0511_F2_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F2 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_0512_F2_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F2 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F2, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0513_F3_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0514_F3_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0515_F3_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0516_F3_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0517_F3_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0518_F3_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0519_F3_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0520_F3_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0521_F3_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0522_F3_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0523_F3_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0524_F3_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0525_F3_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0526_F3_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0527_F3_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0528_F3_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0529_F3_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0530_F3_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0531_F3_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0532_F3_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0533_F3_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0534_F3_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0535_F3_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0536_F3_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0537_F3_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0538_F3_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0539_F3_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0540_F3_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0541_F3_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0542_F3_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0543_F3_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0544_F3_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0545_F3_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0546_F3_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0547_F3_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0548_F3_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0549_F3_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_0550_F3_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0551_F3_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_0552_F3_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0553_F3_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0554_F3_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0555_F3_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0556_F3_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0557_F3_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_0558_F3_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0559_F3_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_0560_F3_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0561_F3_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0562_F3_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0563_F3_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0564_F3_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0565_F3_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_0566_F3_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0567_F3_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_0568_F3_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0569_F3_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0570_F3_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0571_F3_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0572_F3_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0573_F3_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_0574_F3_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0575_F3_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_0576_F3_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0577_F3_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0578_F3_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0579_F3_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0580_F3_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0581_F3_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0582_F3_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0583_F3_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0584_F3_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0585_F3_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0586_F3_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0587_F3_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0588_F3_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0589_F3_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0590_F3_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0591_F3_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0592_F3_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0593_F3_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0594_F3_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0595_F3_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0596_F3_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0597_F3_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0598_F3_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0599_F3_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0600_F3_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0601_F3_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_0602_F3_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0603_F3_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_0604_F3_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0605_F3_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_0606_F3_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0607_F3_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_0608_F3_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0609_F3_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0610_F3_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0611_F3_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0612_F3_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0613_F3_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0614_F3_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0615_F3_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0616_F3_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0617_F3_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0618_F3_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0619_F3_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0620_F3_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0621_F3_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0622_F3_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0623_F3_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0624_F3_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0625_F3_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0626_F3_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0627_F3_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0628_F3_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0629_F3_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0630_F3_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0631_F3_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0632_F3_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0633_F3_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_0634_F3_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0635_F3_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_0636_F3_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0637_F3_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,6) = exp(-v) - log(6) < 2 since exp(-v) < 2+log(6)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_0638_F3_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0639_F3_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F3(v,3) = exp(-v) - log(3) < 2 since exp(-v) < 2+log(3)
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_0640_F3_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0641_F3_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0642_F3_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0643_F3_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0644_F3_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0645_F3_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0646_F3_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0647_F3_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0648_F3_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0649_F3_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0650_F3_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0651_F3_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0652_F3_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0653_F3_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0654_F3_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0655_F3_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0656_F3_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0657_F3_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_
_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0748_F3_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0749_F3_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0750_F3_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0751_F3_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0752_F3_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0753_F3_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0754_F3_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0755_F3_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0756_F3_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0757_F3_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0758_F3_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0759_F3_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0760_F3_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0761_F3_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0762_F3_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0763_F3_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0764_F3_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0765_F3_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0766_F3_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F3 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0767_F3_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F3 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0768_F3_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F3 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F3, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0769_F4_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0770_F4_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0771_F4_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0772_F4_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0773_F4_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0774_F4_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0775_F4_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0776_F4_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0777_F4_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0778_F4_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0779_F4_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0780_F4_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0781_F4_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0782_F4_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0783_F4_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0784_F4_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0785_F4_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0786_F4_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0787_F4_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0788_F4_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0789_F4_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0790_F4_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0791_F4_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0792_F4_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0793_F4_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0794_F4_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0795_F4_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0796_F4_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0797_F4_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0798_F4_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0799_F4_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0800_F4_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0801_F4_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0802_F4_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0803_F4_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0804_F4_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0805_F4_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F3 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0806_F4_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0807_F4_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F3 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0808_F4_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0809_F4_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0810_F4_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0811_F4_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0812_F4_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0813_F4_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F3 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0814_F4_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0815_F4_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F3 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0816_F4_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0817_F4_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0818_F4_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0819_F4_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0820_F4_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0821_F4_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F4 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0822_F4_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0823_F4_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F4 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0824_F4_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0825_F4_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0826_F4_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0827_F4_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0828_F4_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0829_F4_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F4 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0830_F4_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0831_F4_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F4 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0832_F4_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0833_F4_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0834_F4_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0835_F4_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0836_F4_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0837_F4_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0838_F4_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0839_F4_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0840_F4_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0841_F4_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0842_F4_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0843_F4_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0844_F4_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0845_F4_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0846_F4_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0847_F4_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0848_F4_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0849_F4_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0850_F4_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0851_F4_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0852_F4_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0853_F4_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0854_F4_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0855_F4_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0856_F4_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0857_F4_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0858_F4_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0859_F4_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0860_F4_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0861_F4_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0862_F4_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0863_F4_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_0864_F4_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0865_F4_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0866_F4_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0867_F4_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0868_F4_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0869_F4_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0870_F4_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0871_F4_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0872_F4_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0873_F4_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0874_F4_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0875_F4_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0876_F4_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0877_F4_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0878_F4_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0879_F4_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0880_F4_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0881_F4_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0882_F4_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0883_F4_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0884_F4_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0885_F4_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_0886_F4_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F4 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_0887_F4_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_0888_F4_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F4 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_0889_F4_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F4 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F4, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F4 outer, F8 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real
y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1055_F5_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1056_F5_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1057_F5_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1058_F5_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log6_gt_1]

private lemma circ_1059_F5_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1060_F5_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log3_gt_1]

private lemma circ_1061_F5_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1062_F5_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log6_gt_1]

private lemma circ_1063_F5_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1064_F5_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log3_gt_1]

private lemma circ_1065_F5_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1066_F5_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log6_gt_1]

private lemma circ_1067_F5_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1068_F5_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log3_gt_1]

private lemma circ_1069_F5_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1070_F5_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log6_gt_1]

private lemma circ_1071_F5_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1072_F5_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log3_gt_1]

private lemma circ_1073_F5_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1074_F5_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log6_gt_1]

private lemma circ_1075_F5_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1076_F5_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log3_gt_1]

private lemma circ_1077_F5_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1078_F5_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log6_gt_1]

private lemma circ_1079_F5_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1080_F5_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log3_gt_1]

private lemma circ_1081_F5_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1082_F5_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log6_gt_1]

private lemma circ_1083_F5_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1084_F5_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log3_gt_1]

private lemma circ_1085_F5_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1086_F5_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log6_gt_1]

private lemma circ_1087_F5_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1088_F5_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log3_gt_1]

private lemma circ_1089_F5_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1090_F5_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1091_F5_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1092_F5_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1093_F5_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1094_F5_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1095_F5_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1096_F5_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1097_F5_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1098_F5_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1099_F5_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1100_F5_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1101_F5_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1102_F5_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1103_F5_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1104_F5_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1105_F5_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1106_F5_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log6_gt_1]

private lemma circ_1107_F5_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1108_F5_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 6), log3_gt_1]

private lemma circ_1109_F5_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1110_F5_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log6_gt_1]

private lemma circ_1111_F5_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1112_F5_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 6), log3_gt_1]

private lemma circ_1113_F5_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1114_F5_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log6_gt_1]

private lemma circ_1115_F5_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1116_F5_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log 3), log3_gt_1]

private lemma circ_1117_F5_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1118_F5_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log6_gt_1]

private lemma circ_1119_F5_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1120_F5_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log 3), log3_gt_1]

private lemma circ_1121_F5_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1122_F5_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1123_F5_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1124_F5_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1125_F5_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1126_F5_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1127_F5_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1128_F5_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1129_F5_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1130_F5_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1131_F5_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1132_F5_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1133_F5_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1134_F5_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1135_F5_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1136_F5_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1137_F5_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1138_F5_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log6_gt_1]

private lemma circ_1139_F5_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1140_F5_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-6)), log3_gt_1]

private lemma circ_1141_F5_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1142_F5_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log6_gt_1]

private lemma circ_1143_F5_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1144_F5_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-6)), log3_gt_1]

private lemma circ_1145_F5_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1146_F5_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log6_gt_1]

private lemma circ_1147_F5_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1148_F5_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-6) - Real.log (-3)), log3_gt_1]

private lemma circ_1149_F5_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1150_F5_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner < 0 so exp(inner) < 1 and log(c) > 1; result < 0
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log6_gt_1]

private lemma circ_1151_F5_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1152_F5_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(small_inner) small relative to log(c) > 1; result < 2
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_pos (Real.exp (-3) - Real.log (-3)), log3_gt_1]

private lemma circ_1153_F5_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1154_F5_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1155_F5_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1156_F5_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1157_F5_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1158_F5_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1159_F5_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1160_F5_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1161_F5_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1162_F5_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F5: exp(c) - log(inner). Result 1.5558.
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1163_F5_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1164_F5_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F5: exp(c) - log(inner). Result 2.2490.
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_1165_F5_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1166_F5_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1167_F5_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1168_F5_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F5 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result > 3 since exp(inner) > exp(3) > 19 and log(c) < 2
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1169_F5_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F5 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F5, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1170_F5_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F5 x (D_
y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1327_F6_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1328_F6_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1329_F6_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1330_F6_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1331_F6_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1332_F6_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1333_F6_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1334_F6_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1335_F6_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1336_F6_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1337_F6_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1338_F6_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1339_F6_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1340_F6_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1341_F6_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1342_F6_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1343_F6_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1344_F6_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1345_F6_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1346_F6_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1347_F6_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1348_F6_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1349_F6_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1350_F6_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1351_F6_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1352_F6_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1353_F6_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1354_F6_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1355_F6_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1356_F6_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1357_F6_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1358_F6_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1359_F6_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1360_F6_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1361_F6_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1362_F6_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1363_F6_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1364_F6_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1365_F6_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1366_F6_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1367_F6_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1368_F6_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1369_F6_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1370_F6_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1371_F6_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1372_F6_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1373_F6_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1374_F6_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1375_F6_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1376_F6_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1377_F6_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1378_F6_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1379_F6_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1380_F6_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1381_F6_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1382_F6_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1383_F6_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1384_F6_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1385_F6_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1386_F6_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1387_F6_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1388_F6_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1389_F6_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1390_F6_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1391_F6_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1392_F6_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1393_F6_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1394_F6_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1395_F6_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1396_F6_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1397_F6_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1398_F6_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1399_F6_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1400_F6_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1401_F6_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1402_F6_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1403_F6_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1404_F6_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1405_F6_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1406_F6_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-6) - log(inner) << 2. exp(-6) < 1 < 2.
  nlinarith [Real.exp_pos ((-6:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1407_F6_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1408_F6_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F6 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F6: exp(-3) - log(inner) << 2. exp(-3) < 1 < 2.
  nlinarith [Real.exp_pos ((-3:ℝ)), Real.exp_one_gt_d9, exp3_gt_19,
             Real.exp_add 1 1, Real.exp_add 1 2]

private lemma circ_1409_F6_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F6 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1410_F6_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F6 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F6, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1883_F8_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1884_F8_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1885_F8_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1886_F8_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1887_F8_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1888_F8_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F6 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1889_F8_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1890_F8_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1891_F8_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1892_F8_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1893_F8_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1894_F8_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1895_F8_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1896_F8_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1897_F8_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1898_F8_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1899_F8_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1900_F8_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1901_F8_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1902_F8_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1903_F8_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1904_F8_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1905_F8_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1906_F8_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1907_F8_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1908_F8_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1909_F8_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1910_F8_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log6_lt_2]

private lemma circ_1911_F8_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1912_F8_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [exp3_gt_19, log3_lt_2]

private lemma circ_1913_F8_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1914_F8_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F8 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1915_F8_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1916_F8_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F8 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1917_F8_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1918_F8_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F8 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1919_F8_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1920_F8_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- Near-2 case for F8 outer, F8 inner
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9, Real.exp_add 1 1,
             Real.exp_add 1 2, Real.exp_add 3 3,
             Real.log_pos (show (1:ℝ) < 3 from by norm_num),
             Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_1921_F8_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1922_F8_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1923_F8_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1924_F8_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1925_F8_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1926_F8_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1927_F8_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1928_F8_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1929_F8_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1930_F8_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1931_F8_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1932_F8_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1933_F8_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1934_F8_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1935_F8_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1936_F8_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1937_F8_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1938_F8_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1939_F8_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1940_F8_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1941_F8_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1942_F8_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1943_F8_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1944_F8_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1945_F8_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1946_F8_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_1947_F8_F10_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F10 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- result < 1: exp(-v) small and log subtracted
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_1948_F8_F10_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_
x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2037_F8_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2038_F8_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2039_F8_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2040_F8_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2041_F8_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2042_F8_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2043_F8_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2044_F8_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2045_F8_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2046_F8_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F8 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2047_F8_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F8 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2048_F8_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F8 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F8, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- exp(-inner) < 1 and log(c) > 1, so result < 0 < 2
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2049_F9_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2050_F9_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2051_F9_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2052_F9_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2053_F9_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2054_F9_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2055_F9_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2056_F9_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2057_F9_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2058_F9_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2059_F9_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2060_F9_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2061_F9_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2062_F9_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2063_F9_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2064_F9_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2065_F9_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2066_F9_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2067_F9_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2068_F9_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2069_F9_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2070_F9_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2071_F9_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2072_F9_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2073_F9_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2074_F9_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2075_F9_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2076_F9_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2077_F9_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2078_F9_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2079_F9_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2080_F9_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2081_F9_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2082_F9_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2083_F9_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2084_F9_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2085_F9_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2086_F9_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2087_F9_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2088_F9_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2089_F9_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2090_F9_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2091_F9_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2092_F9_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2093_F9_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2094_F9_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2095_F9_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2096_F9_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2097_F9_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2098_F9_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2099_F9_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2100_F9_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2101_F9_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2102_F9_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2103_F9_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2104_F9_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2105_F9_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2106_F9_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2107_F9_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2108_F9_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2109_F9_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2110_F9_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2111_F9_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2112_F9_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2113_F9_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2114_F9_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2115_F9_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2116_F9_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2117_F9_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2118_F9_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2119_F9_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2120_F9_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2121_F9_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2122_F9_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2123_F9_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2124_F9_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2125_F9_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2126_F9_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2127_F9_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2128_F9_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2129_F9_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2130_F9_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2131_F9_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2132_F9_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2133_F9_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2134_F9_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2135_F9_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2136_F9_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2137_F9_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2138_F9_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2139_F9_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2140_F9_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2141_F9_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2142_F9_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2143_F9_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2144_F9_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2145_F9_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2146_F9_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2147_F9_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2148_F9_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2149_F9_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2150_F9_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2151_F9_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2152_F9_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2153_F9_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2154_F9_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2155_F9_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2156_F9_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2157_F9_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2158_F9_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2159_F9_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2160_F9_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2161_F9_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2162_F9_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2163_F9_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2164_F9_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2165_F9_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2166_F9_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2167_F9_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2168_F9_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2169_F9_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2170_F9_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2171_F9_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2172_F9_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2173_F9_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2174_F9_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2175_F9_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2176_F9_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2177_F9_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.208, 6) > 2 since 4.208 > 2+log(6)
  linarith [log6_lt_2]

private lemma circ_2178_F9_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2179_F9_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.208, 3) > 2 since 4.208 > 2+log(3)
  linarith [log3_lt_2]

private lemma circ_2180_F9_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2181_F9_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.901, 6) > 2 since 4.901 > 2+log(6)
  linarith [log6_lt_2]

private lemma circ_2182_F9_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2183_F9_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2184_F9_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2185_F9_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2186_F9_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2187_F9_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2188_F9_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2189_F9_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2190_F9_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2191_F9_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2192_F9_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2193_F9_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.208, 6) > 2 since 4.208 > 2+log(6)
  linarith [log6_lt_2]

private lemma circ_2194_F9_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2195_F9_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.208, 3) > 2 since 4.208 > 2+log(3)
  linarith [log3_lt_2]

private lemma circ_2196_F9_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2197_F9_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(4.901, 6) > 2 since 4.901 > 2+log(6)
  linarith [log6_lt_2]

private lemma circ_2198_F9_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2199_F9_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2200_F9_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2201_F9_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2202_F9_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2203_F9_F10_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2204_F9_F10_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2205_F9_F10_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2206_F9_F10_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2207_F9_F10_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F10 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2208_F9_F10_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2209_F9_F11_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2210_F9_F11_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2211_F9_F11_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2212_F9_F11_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2213_F9_F11_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2214_F9_F11_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2215_F9_F11_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2216_F9_F11_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2217_F9_F11_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3.2614, 6) < 2 since 3.2614 < 2+log(6)
  linarith [log6_gt_1]

private lemma circ_2218_F9_F11_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2219_F9_F11_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(log(exp(3)+6), 3) > 2: log(exp(3)+6) > 2+log(3)
  -- ↔ exp(3)+6 > exp(2)*3 = 3*exp(2)
  -- exp(3)+3 > 3*exp(2)-3 since exp(3)=exp(1)*exp(2) > 2.718*7.38 > 20.06 > 19.17
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have he3 : Real.exp 3 = Real.exp 1 * Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add, ← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_lt_log (by linarith [Real.exp_pos 3] : (0:ℝ) < Real.exp 3 + 6)
               (show Real.exp 3 + 6 < Real.exp 2 * 3 from by nlinarith),
             Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2220_F9_F11_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2221_F9_F11_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3.1392, 6) < 2 since 3.1392 < 2+log(6)
  linarith [log6_gt_1]

private lemma circ_2222_F9_F11_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2223_F9_F11_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F11 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(log(exp(3)+3), 3) > 2: log(exp(3)+3) > 2+log(3)
  -- ↔ exp(3)+3 > exp(2)*3 = 3*exp(2)
  -- exp(3)+3 > 3*exp(2)-3 since exp(3)=exp(1)*exp(2) > 2.718*7.38 > 20.06 > 19.17
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have he3 : Real.exp 3 = Real.exp 1 * Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add, ← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.log_lt_log (by linarith [Real.exp_pos 3] : (0:ℝ) < Real.exp 3 + 3)
               (show Real.exp 3 + 3 < Real.exp 2 * 3 from by nlinarith),
             Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2224_F9_F11_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2225_F9_F12_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2226_F9_F12_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2227_F9_F12_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2228_F9_F12_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2229_F9_F12_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2230_F9_F12_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2231_F9_F12_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2232_F9_F12_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2233_F9_F12_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2234_F9_F12_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2235_F9_F12_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(2.6451, 3) < 2 since 2.6451 < 2+log(3)
  linarith [log3_gt_1]

private lemma circ_2236_F9_F12_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) > 2 since log(inner) < 1
  linarith [log3_lt_2]

private lemma circ_2237_F9_F12_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(2.8382, 6) < 2 since 2.8382 < 2+log(6)
  linarith [log6_gt_1]

private lemma circ_2238_F9_F12_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2239_F9_F12_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F12 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(2.8382, 3) < 2 since 2.8382 < 2+log(3)
  linarith [log3_gt_1]

private lemma circ_2240_F9_F12_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(3, inner) < 2 since log(inner) > 1
  linarith [log3_gt_1]

private lemma circ_2241_F9_F13_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2242_F9_F13_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2243_F9_F13_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2244_F9_F13_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2245_F9_F13_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2246_F9_F13_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2247_F9_F13_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2248_F9_F13_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2249_F9_F13_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2250_F9_F13_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log6_gt_1]

private lemma circ_2251_F9_F13_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2252_F9_F13_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2253_F9_F13_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2254_F9_F13_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2255_F9_F13_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F13 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2256_F9_F13_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2257_F9_F14_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2258_F9_F14_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2259_F9_F14_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2260_F9_F14_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2261_F9_F14_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2262_F9_F14_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2263_F9_F14_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2264_F9_F14_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2265_F9_F14_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2266_F9_F14_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) < 2 since log(inner) > 4
  linarith [log6_gt_1]

private lemma circ_2267_F9_F14_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2268_F9_F14_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2269_F9_F14_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2270_F9_F14_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) < 2 since log(inner) > 4
  linarith [log6_gt_1]

private lemma circ_2271_F9_F14_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F14 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2272_F9_F14_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2273_F9_F15_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2274_F9_F15_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2275_F9_F15_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2276_F9_F15_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2277_F9_F15_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2278_F9_F15_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2279_F9_F15_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2280_F9_F15_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2281_F9_F15_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2282_F9_F15_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) < 2 since log(inner) > 4
  linarith [log6_gt_1]

private lemma circ_2283_F9_F15_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2284_F9_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2285_F9_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2286_F9_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) < 2 since log(inner) > 4
  linarith [log6_gt_1]

private lemma circ_2287_F9_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2288_F9_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2289_F9_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2290_F9_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2291_F9_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2292_F9_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2293_F9_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2294_F9_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2295_F9_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2296_F9_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2297_F9_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2298_F9_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F9(6, inner) > 2 since log(inner) < 4
  linarith [log6_lt_2]

private lemma circ_2299_F9_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2300_F9_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2301_F9_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2302_F9_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F9 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2303_F9_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F9 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2304_F9_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F9 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F9, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [log3_gt_1]

private lemma circ_2305_F10_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2306_F10_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2307_F10_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2308_F10_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2309_F10_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2310_F10_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2311_F10_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2312_F10_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2313_F10_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2314_F10_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.2938.
  linarith [log6_lt_2]

private lemma circ_2315_F10_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2316_F10_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2317_F10_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2318_F10_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.9869.
  linarith [log6_lt_2]

private lemma circ_2319_F10_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2320_F10_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2321_F10_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2322_F10_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2323_F10_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2324_F10_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2325_F10_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2326_F10_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2327_F10_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2328_F10_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2329_F10_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2330_F10_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.2938.
  linarith [log6_lt_2]

private lemma circ_2331_F10_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2332_F10_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2333_F10_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2334_F10_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.9869.
  linarith [log6_lt_2]

private lemma circ_2335_F10_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2336_F10_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2337_F10_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2338_F10_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2339_F10_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2340_F10_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7893.
  linarith [log3_lt_2]

private lemma circ_2341_F10_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2342_F10_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2343_F10_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2344_F10_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0961.
  linarith [log3_lt_2]

private lemma circ_2345_F10_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2346_F10_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2347_F10_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2348_F10_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7420.
  linarith [log3_lt_2]

private lemma circ_2349_F10_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2350_F10_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2351_F10_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2352_F10_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0488.
  linarith [log3_lt_2]

private lemma circ_2353_F10_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2354_F10_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2355_F10_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2356_F10_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7893.
  linarith [log3_lt_2]

private lemma circ_2357_F10_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2358_F10_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2359_F10_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2360_F10_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0961.
  linarith [log3_lt_2]

private lemma circ_2361_F10_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2362_F10_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2363_F10_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2364_F10_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7420.
  linarith [log3_lt_2]

private lemma circ_2365_F10_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2366_F10_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2367_F10_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2368_F10_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0488.
  linarith [log3_lt_2]

private lemma circ_2369_F10_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2370_F10_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2371_F10_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2372_F10_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2373_F10_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2374_F10_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.2938.
  linarith [log6_lt_2]

private lemma circ_2375_F10_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2376_F10_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2377_F10_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2378_F10_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2379_F10_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2380_F10_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2381_F10_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2382_F10_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.9869.
  linarith [log6_lt_2]

private lemma circ_2383_F10_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2384_F10_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2385_F10_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2386_F10_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2387_F10_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2388_F10_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7893.
  linarith [log3_lt_2]

private lemma circ_2389_F10_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2390_F10_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2391_F10_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2392_F10_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7420.
  linarith [log3_lt_2]

private lemma circ_2393_F10_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2394_F10_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2395_F10_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2396_F10_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0961.
  linarith [log3_lt_2]

private lemma circ_2397_F10_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2398_F10_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2399_F10_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2400_F10_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0488.
  linarith [log3_lt_2]

private lemma circ_2401_F10_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2402_F10_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2403_F10_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2404_F10_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2405_F10_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2406_F10_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.2938.
  linarith [log6_lt_2]

private lemma circ_2407_F10_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2408_F10_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2409_F10_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2410_F10_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2411_F10_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2412_F10_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2413_F10_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2414_F10_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.9869.
  linarith [log6_lt_2]

private lemma circ_2415_F10_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2416_F10_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2417_F10_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2418_F10_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2419_F10_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2420_F10_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7893.
  linarith [log3_lt_2]

private lemma circ_2421_F10_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2422_F10_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2423_F10_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2424_F10_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.7420.
  linarith [log3_lt_2]

private lemma circ_2425_F10_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2426_F10_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2427_F10_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2428_F10_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0961.
  linarith [log3_lt_2]

private lemma circ_2429_F10_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2430_F10_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2431_F10_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2432_F10_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = -1.0488.
  linarith [log3_lt_2]

private lemma circ_2433_F10_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 4.2082.
  linarith [log6_lt_2]

private lemma circ_2434_F10_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2435_F10_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 4.2082.
  linarith [log3_lt_2]

private lemma circ_2436_F10_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 4.2082.
  linarith [log3_gt_1]

private lemma circ_2437_F10_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 4.9014.
  linarith [log6_lt_2]

private lemma circ_2438_F10_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2439_F10_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2440_F10_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 4.9014.
  linarith [log3_gt_1]

private lemma circ_2441_F10_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2442_F10_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2443_F10_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2444_F10_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 1.2082.
  linarith [log3_lt_2]

private lemma circ_2445_F10_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2446_F10_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2447_F10_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2448_F10_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 1.9014.
  linarith [log3_lt_2]

private lemma circ_2449_F10_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 4.2082.
  linarith [log6_lt_2]

private lemma circ_2450_F10_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2451_F10_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 4.2082.
  linarith [log3_lt_2]

private lemma circ_2452_F10_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 4.2082.
  linarith [log3_gt_1]

private lemma circ_2453_F10_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 4.9014.
  linarith [log6_lt_2]

private lemma circ_2454_F10_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2455_F10_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2456_F10_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 4.9014.
  linarith [log3_gt_1]

private lemma circ_2457_F10_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2458_F10_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2459_F10_F10_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2460_F10_F10_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 1.2082.
  linarith [log3_lt_2]

private lemma circ_2461_F10_F10_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2462_F10_F10_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2463_F10_F10_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F10 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2464_F10_F10_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 1.9014.
  linarith [log3_lt_2]

private lemma circ_2465_F10_F11_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2466_F10_F11_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2467_F10_F11_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2468_F10_F11_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 6.0148.
  linarith [log3_gt_1]

private lemma circ_2469_F10_F11_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2470_F10_F11_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2471_F10_F11_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2472_F10_F11_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 6.0074.
  linarith [log3_gt_1]

private lemma circ_2473_F10_F11_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 3.2614.
  linarith [log6_gt_1]

private lemma circ_2474_F10_F11_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2475_F10_F11_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 3.2614.
  linarith [log3_lt_2]

private lemma circ_2476_F10_F11_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 3.2614.
  linarith [log3_gt_1]

private lemma circ_2477_F10_F11_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 3.1392.
  linarith [log6_gt_1]

private lemma circ_2478_F10_F11_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2479_F10_F11_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F11 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 3.1392.
  linarith [log3_lt_2]

private lemma circ_2480_F10_F11_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 3.1392.
  linarith [log3_gt_1]

private lemma circ_2481_F10_F12_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2482_F10_F12_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2483_F10_F12_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2484_F10_F12_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 5.9850.
  linarith [log3_gt_1]

private lemma circ_2485_F10_F12_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2486_F10_F12_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2487_F10_F12_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2488_F10_F12_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 5.9925.
  linarith [log3_gt_1]

private lemma circ_2489_F10_F12_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2490_F10_F12_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2491_F10_F12_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 2.6451.
  linarith [log3_gt_1]

private lemma circ_2492_F10_F12_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) > 2. inner = 2.6451.
  linarith [log3_lt_2]

private lemma circ_2493_F10_F12_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 2.8382.
  linarith [log6_gt_1]

private lemma circ_2494_F10_F12_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2495_F10_F12_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F12 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 2.8382.
  linarith [log3_gt_1]

private lemma circ_2496_F10_F12_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(3) < 2. inner = 2.8382.
  linarith [log3_gt_1]

private lemma circ_2497_F10_F13_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2498_F10_F13_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2499_F10_F13_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2500_F10_F13_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2501_F10_F13_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2502_F10_F13_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2503_F10_F13_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2504_F10_F13_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2505_F10_F13_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2506_F10_F13_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2507_F10_F13_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2508_F10_F13_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2509_F10_F13_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2510_F10_F13_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 27.0000.
  linarith [log6_lt_2]

private lemma circ_2511_F10_F13_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F13 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2512_F10_F13_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2513_F10_F14_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2514_F10_F14_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2515_F10_F14_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2516_F10_F14_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2517_F10_F14_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2518_F10_F14_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2519_F10_F14_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2520_F10_F14_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2521_F10_F14_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2522_F10_F14_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 120.5132.
  linarith [log6_gt_1]

private lemma circ_2523_F10_F14_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2524_F10_F14_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2525_F10_F14_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2526_F10_F14_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 60.2566.
  linarith [log6_gt_1]

private lemma circ_2527_F10_F14_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F14 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2528_F10_F14_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2529_F10_F15_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2530_F10_F15_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2531_F10_F15_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2532_F10_F15_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2533_F10_F15_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2534_F10_F15_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2535_F10_F15_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2536_F10_F15_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2537_F10_F15_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2538_F10_F15_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 120.5132.
  linarith [log6_gt_1]

private lemma circ_2539_F10_F15_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2540_F10_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2541_F10_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2542_F10_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) < 2. inner = 60.2566.
  linarith [log6_gt_1]

private lemma circ_2543_F10_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2544_F10_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2545_F10_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2546_F10_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 36.0000.
  linarith [log6_lt_2]

private lemma circ_2547_F10_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2548_F10_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2549_F10_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2550_F10_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.0000.
  linarith [log6_lt_2]

private lemma circ_2551_F10_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2552_F10_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2553_F10_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2554_F10_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F10: after log_neg_eq_log, inner - log(6) > 2. inner = 18.0000.
  linarith [log6_lt_2]

private lemma circ_2555_F10_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2556_F10_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2557_F10_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2558_F10_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F10 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log6_lt_2]

private lemma circ_2559_F10_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F10 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- inner_val > 3.5 and log(c) < 2; result > 1.5
  linarith [log3_lt_2]

private lemma circ_2560_F10_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F10 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F10, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2561_F11_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2562_F11_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2563_F11_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2564_F11_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2565_F11_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2566_F11_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2567_F11_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2568_F11_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2569_F11_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2570_F11_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2571_F11_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2572_F11_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2573_F11_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2574_F11_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2575_F11_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2576_F11_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2577_F11_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2578_F11_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2579_F11_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2580_F11_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2581_F11_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2582_F11_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2583_F11_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2584_F11_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2585_F11_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2586_F11_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2587_F11_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2588_F11_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2589_F11_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2590_F11_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2591_F11_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2592_F11_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2593_F11_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 6) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 6) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2594_F11_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2595_F11_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 6) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 6) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2596_F11_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2597_F11_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 3) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 3) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2598_F11_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2599_F11_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 3) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 3) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2600_F11_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2601_F11_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 6) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 6) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2602_F11_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2603_F11_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 6) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 6) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2604_F11_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2605_F11_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 3) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 3) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2606_F11_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2607_F11_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 3) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 3) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2608_F11_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2609_F11_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-6)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-6)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2610_F11_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2611_F11_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-6)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-6)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2612_F11_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2613_F11_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-3)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-3)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2614_F11_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2615_F11_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-3)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-3)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2616_F11_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2617_F11_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-6)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-6)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2618_F11_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2619_F11_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-6)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-6)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2620_F11_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2621_F11_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-3)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-3)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2622_F11_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2623_F11_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-3)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-3)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2624_F11_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2625_F11_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2626_F11_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2627_F11_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2628_F11_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2629_F11_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2630_F11_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2631_F11_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2632_F11_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2633_F11_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2634_F11_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2635_F11_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2636_F11_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2637_F11_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2638_F11_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2639_F11_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2640_F11_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2641_F11_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 6) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 6) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2642_F11_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2643_F11_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 6) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 6) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2644_F11_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2645_F11_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 6) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 6) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2646_F11_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2647_F11_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 6) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 6) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2648_F11_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2649_F11_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 3) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 3) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2650_F11_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2651_F11_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log 3) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log 3) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2652_F11_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2653_F11_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 3) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 3) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2654_F11_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2655_F11_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log 3) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log 3) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2656_F11_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2657_F11_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2658_F11_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2659_F11_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2660_F11_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2661_F11_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2662_F11_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2663_F11_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2664_F11_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2665_F11_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2666_F11_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2667_F11_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2668_F11_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2669_F11_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2670_F11_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2671_F11_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2672_F11_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2673_F11_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-6)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-6)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2674_F11_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2675_F11_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-6)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-6)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2676_F11_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2677_F11_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-6)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-6)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2678_F11_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2679_F11_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-6)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-6)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2680_F11_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2681_F11_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-3)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-3)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2682_F11_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2683_F11_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-6) - Real.log (-3)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-6) - Real.log (-3)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2684_F11_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2685_F11_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) < 2 since exp(v)+6 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+6) = 2 → exp(v)+6 = exp(2)
  -- But exp(v)+6 < exp(2): exp(v) < exp(2)-6 < 7.39-6
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-3)) + 6 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-3)) + 6 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2686_F11_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2687_F11_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3)),
             Real.log_lt_log (show (0:ℝ) < Real.exp (Real.exp (-3) - Real.log (-3)) + 3 from by positivity)
                           (show Real.exp (Real.exp (-3) - Real.log (-3)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2688_F11_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2689_F11_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2690_F11_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2691_F11_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2692_F11_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log 6)]

private lemma circ_2693_F11_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2694_F11_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2695_F11_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2696_F11_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log 3)]

private lemma circ_2697_F11_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 6)]

private lemma circ_2698_F11_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2699_F11_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 6),
             Real.log_lt_log (show (0:ℝ) < Real.exp ((3 : ℝ) - Real.log 6) + 3 from by positivity)
                           (show Real.exp ((3 : ℝ) - Real.log 6) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2700_F11_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 6)]

private lemma circ_2701_F11_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 3)]

private lemma circ_2702_F11_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2703_F11_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 3)]

private lemma circ_2704_F11_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 3)]

private lemma circ_2705_F11_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2706_F11_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2707_F11_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2708_F11_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log (-6))]

private lemma circ_2709_F11_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2710_F11_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2711_F11_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2712_F11_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log (-3))]

private lemma circ_2713_F11_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-6))]

private lemma circ_2714_F11_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2715_F11_F10_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) < 2 since exp(v)+3 < exp(2) ≈ 7.39
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  have hlog2 : Real.log (Real.exp 2) = 2 := Real.log_exp 2
  -- key: log(exp(v)+3) = 2 → exp(v)+3 = exp(2)
  -- But exp(v)+3 < exp(2): exp(v) < exp(2)-3 < 7.39-3
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-6)),
             Real.log_lt_log (show (0:ℝ) < Real.exp ((3 : ℝ) - Real.log (-6)) + 3 from by positivity)
                           (show Real.exp ((3 : ℝ) - Real.log (-6)) + 3 < Real.exp 2 from by
                             nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9])]

private lemma circ_2716_F11_F10_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-6))]

private lemma circ_2717_F11_F10_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-3))]

private lemma circ_2718_F11_F10_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2719_F11_F10_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F10 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-3))]

private lemma circ_2720_F11_F10_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-3))]

private lemma circ_2721_F11_F11_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2722_F11_F11_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2723_F11_F11_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2724_F11_F11_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 + 6))]

private lemma circ_2725_F11_F11_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2726_F11_F11_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2727_F11_F11_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2728_F11_F11_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 + 3))]

private lemma circ_2729_F11_F11_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2730_F11_F11_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2731_F11_F11_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2732_F11_F11_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2733_F11_F11_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2734_F11_F11_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2735_F11_F11_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F11 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2736_F11_F11_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2737_F11_F12_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2738_F11_F12_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2739_F11_F12_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2740_F11_F12_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 - 6))]

private lemma circ_2741_F11_F12_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2742_F11_F12_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2743_F11_F12_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2744_F11_F12_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 - 3))]

private lemma circ_2745_F11_F12_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_2746_F11_F12_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2747_F11_F12_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_2748_F11_F12_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_2749_F11_F12_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+6) > 2 since exp(v)+6 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_2750_F11_F12_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2751_F11_F12_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F12 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_2752_F11_F12_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_2753_F11_F13_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2754_F11_F13_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2755_F11_F13_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2756_F11_F13_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2757_F11_F13_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2758_F11_F13_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2759_F11_F13_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2760_F11_F13_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2761_F11_F13_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2762_F11_F13_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2763_F11_F13_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2764_F11_F13_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2765_F11_F13_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2766_F11_F13_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2767_F11_F13_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F13 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2768_F11_F13_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2769_F11_F14_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2770_F11_F14_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2771_F11_F14_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2772_F11_F14_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2773_F11_F14_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2774_F11_F14_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2775_F11_F14_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2776_F11_F14_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2777_F11_F14_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2778_F11_F14_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2779_F11_F14_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2780_F11_F14_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2781_F11_F14_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2782_F11_F14_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2783_F11_F14_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F14 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2784_F11_F14_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2785_F11_F15_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2786_F11_F15_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2787_F11_F15_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2788_F11_F15_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2789_F11_F15_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2790_F11_F15_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2791_F11_F15_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2792_F11_F15_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2793_F11_F15_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2794_F11_F15_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2795_F11_F15_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2796_F11_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2797_F11_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2798_F11_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2799_F11_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2800_F11_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2801_F11_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2802_F11_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2803_F11_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2804_F11_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2805_F11_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2806_F11_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2807_F11_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2808_F11_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2809_F11_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2810_F11_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2811_F11_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2812_F11_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2813_F11_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2814_F11_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F11 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2815_F11_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F11 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2816_F11_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F11 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F11, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F11: log(exp(v)+3) > 2 since exp(v)+3 > exp(2)
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (Real.log 3 + Real.log 3))]

private lemma circ_2817_F12_F1_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2818_F12_F1_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2819_F12_F1_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2820_F12_F1_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F1 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2821_F12_F1_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2822_F12_F1_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2823_F12_F1_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2824_F12_F1_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F1 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2825_F12_F1_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2826_F12_F1_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2827_F12_F1_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2828_F12_F1_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F1 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2829_F12_F1_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2830_F12_F1_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2831_F12_F1_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F1 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2832_F12_F1_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F1 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F1] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2833_F12_F2_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2834_F12_F2_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2835_F12_F2_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2836_F12_F2_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F2 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2837_F12_F2_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2838_F12_F2_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2839_F12_F2_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2840_F12_F2_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F2 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2841_F12_F2_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2842_F12_F2_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2843_F12_F2_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2844_F12_F2_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F2 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2845_F12_F2_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2846_F12_F2_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2847_F12_F2_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F2 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2848_F12_F2_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F2 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F2] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2849_F12_F3_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2850_F12_F3_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2851_F12_F3_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2852_F12_F3_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F3 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2853_F12_F3_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2854_F12_F3_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2855_F12_F3_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2856_F12_F3_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F3 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2857_F12_F3_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2858_F12_F3_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2859_F12_F3_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2860_F12_F3_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F3 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2861_F12_F3_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2862_F12_F3_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2863_F12_F3_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F3 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2864_F12_F3_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F3 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F3] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2865_F12_F4_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2866_F12_F4_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2867_F12_F4_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2868_F12_F4_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F4 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2869_F12_F4_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2870_F12_F4_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2871_F12_F4_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2872_F12_F4_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F4 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2873_F12_F4_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2874_F12_F4_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2875_F12_F4_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2876_F12_F4_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F4 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2877_F12_F4_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2878_F12_F4_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2879_F12_F4_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F4 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2880_F12_F4_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F4 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F4] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2881_F12_F5_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2882_F12_F5_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2883_F12_F5_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2884_F12_F5_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F5 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2885_F12_F5_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2886_F12_F5_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2887_F12_F5_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2888_F12_F5_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F5 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2889_F12_F5_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2890_F12_F5_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2891_F12_F5_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2892_F12_F5_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F5 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2893_F12_F5_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2894_F12_F5_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2895_F12_F5_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F5 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2896_F12_F5_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F5 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F5] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2897_F12_F6_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2898_F12_F6_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2899_F12_F6_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2900_F12_F6_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F6 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 6)]

private lemma circ_2901_F12_F6_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2902_F12_F6_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2903_F12_F6_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2904_F12_F6_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F6 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 6)]

private lemma circ_2905_F12_F6_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2906_F12_F6_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2907_F12_F6_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2908_F12_F6_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F6 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log 3)]

private lemma circ_2909_F12_F6_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2910_F12_F6_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2911_F12_F6_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F6 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2912_F12_F6_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F6 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F6] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log 3)]

private lemma circ_2913_F12_F7_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2914_F12_F7_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2915_F12_F7_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2916_F12_F7_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F7 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2917_F12_F7_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2918_F12_F7_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2919_F12_F7_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2920_F12_F7_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F7 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2921_F12_F7_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_2922_F12_F7_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2923_F12_F7_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_2924_F12_F7_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F7 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2925_F12_F7_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2926_F12_F7_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2927_F12_F7_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F7 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2928_F12_F7_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F7 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F7] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2929_F12_F8_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2930_F12_F8_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2931_F12_F8_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2932_F12_F8_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F8 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-6))]

private lemma circ_2933_F12_F8_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2934_F12_F8_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2935_F12_F8_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2936_F12_F8_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F8 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-6))]

private lemma circ_2937_F12_F8_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2938_F12_F8_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2939_F12_F8_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2940_F12_F8_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F8 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-6) - Real.log (-3))]

private lemma circ_2941_F12_F8_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) < 2. exp(v)-6 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2942_F12_F8_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2943_F12_F8_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F8 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2944_F12_F8_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F8 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F8] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (-3) - Real.log (-3))]

private lemma circ_2945_F12_F9_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2946_F12_F9_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2947_F12_F9_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2948_F12_F9_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F9 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log 6)]

private lemma circ_2949_F12_F9_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2950_F12_F9_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2951_F12_F9_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2952_F12_F9_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F9 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log 3)]

private lemma circ_2953_F12_F9_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2954_F12_F9_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2955_F12_F9_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2956_F12_F9_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F9 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 6)]

private lemma circ_2957_F12_F9_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2958_F12_F9_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2959_F12_F9_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F9 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 3)]

private lemma circ_2960_F12_F9_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F9 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F9] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log 3)]

private lemma circ_2961_F12_F10_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2962_F12_F10_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2963_F12_F10_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2964_F12_F10_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F10 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log (-6))]

private lemma circ_2965_F12_F10_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2966_F12_F10_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2967_F12_F10_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2968_F12_F10_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F10 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((6 : ℝ) - Real.log (-3))]

private lemma circ_2969_F12_F10_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2970_F12_F10_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2971_F12_F10_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_2972_F12_F10_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F10 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-6))]

private lemma circ_2973_F12_F10_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log6_gt_1]

private lemma circ_2974_F12_F10_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2975_F12_F10_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F10 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-3))]

private lemma circ_2976_F12_F10_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F10 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F10] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos ((3 : ℝ) - Real.log (-3))]

private lemma circ_2977_F12_F11_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2978_F12_F11_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2979_F12_F11_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2980_F12_F11_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F11 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 + 6))]

private lemma circ_2981_F12_F11_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2982_F12_F11_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2983_F12_F11_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2984_F12_F11_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F11 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 + 3))]

private lemma circ_2985_F12_F11_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) > 2. exp(v)-6 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2986_F12_F11_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2987_F12_F11_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2988_F12_F11_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F11 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 6))]

private lemma circ_2989_F12_F11_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) > 2. exp(v)-6 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2990_F12_F11_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2991_F12_F11_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F11 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2992_F12_F11_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F11 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F11] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 + 3))]

private lemma circ_2993_F12_F12_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2994_F12_F12_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2995_F12_F12_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2996_F12_F12_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F12 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 - 6))]

private lemma circ_2997_F12_F12_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2998_F12_F12_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_2999_F12_F12_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3000_F12_F12_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F12 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 6 - 3))]

private lemma circ_3001_F12_F12_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) > 2. exp(v)-6 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_3002_F12_F12_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3003_F12_F12_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_3004_F12_F12_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F12 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 6))]

private lemma circ_3005_F12_F12_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-6) > 2. exp(v)-6 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_3006_F12_F12_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3007_F12_F12_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F12 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_3008_F12_F12_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F12 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F12] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.log (Real.exp 3 - 3))]

private lemma circ_3009_F12_F13_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3010_F12_F13_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3011_F12_F13_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3012_F12_F13_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F13 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3013_F12_F13_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3014_F12_F13_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3015_F12_F13_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3016_F12_F13_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F13 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3017_F12_F13_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3018_F12_F13_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3019_F12_F13_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3020_F12_F13_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F13 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3021_F12_F13_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3022_F12_F13_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3023_F12_F13_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F13 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3024_F12_F13_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F13 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F13] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) < 2. exp(v)-3 < exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (3 * Real.log 3))]

private lemma circ_3025_F12_F14_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3026_F12_F14_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3027_F12_F14_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3028_F12_F14_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F14 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3029_F12_F14_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3030_F12_F14_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3031_F12_F14_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3032_F12_F14_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F14 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3033_F12_F14_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3034_F12_F14_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3035_F12_F14_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3036_F12_F14_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F14 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3037_F12_F14_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3038_F12_F14_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3039_F12_F14_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F14 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3040_F12_F14_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F14 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F14] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3041_F12_F15_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3042_F12_F15_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3043_F12_F15_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3044_F12_F15_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F15 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3045_F12_F15_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3046_F12_F15_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3047_F12_F15_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3048_F12_F15_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F15 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3049_F12_F15_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3050_F12_F15_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3051_F12_F15_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3052_F12_F15_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F15 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3053_F12_F15_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 6 from by norm_num)]

private lemma circ_3054_F12_F15_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3055_F12_F15_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F15 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- result >> 2; contradiction from positivity and bounds
  linarith [exp6_gt_390, Real.log_pos (show (1:ℝ) < 3 from by norm_num)]

private lemma circ_3056_F12_F15_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F15 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F15] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3057_F12_F16_xxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 x x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3058_F12_F16_xxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3059_F12_F16_xxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 x x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3060_F12_F16_xxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F16 x x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (Real.log 6 + Real.log 6))]

private lemma circ_3061_F12_F16_xyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 x y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3062_F12_F16_xyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3063_F12_F16_xyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 x y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3064_F12_F16_xyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F16 x y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_3065_F12_F16_yxx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 y x) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3066_F12_F16_yxx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3067_F12_F16_yxy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 y x) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3068_F12_F16_yxy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F16 y x) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  linarith [Real.exp_pos _, log3_gt_1]

private lemma circ_3069_F12_F16_yyx_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 y y) x = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3070_F12_F16_yyx_B :
    ¬ (∀ x y : ℝ, D_F12 x (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3071_F12_F16_yyy_A :
    ¬ (∀ x y : ℝ, D_F12 (D_F16 y y) y = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- log(exp(inner) + c) > log(exp(3)) = 3 > 2
  linarith [exp3_gt_19, Real.log_pos (show (0:ℝ) < Real.exp 3 from Real.exp_pos _)]

private lemma circ_3072_F12_F16_yyy_B :
    ¬ (∀ x y : ℝ, D_F12 y (D_F16 y y) = x / y) := by
  intro h
  have key := h 6 3
  norm_num at key
  simp only [D_F12, D_F16] at key
  simp only [Real.log_neg_eq_log] at key
  -- F12: log(exp(v)-3) > 2. exp(v)-3 > exp(2).
  have he2 : Real.exp 2 = Real.exp 1 * Real.exp 1 := by
    rw [← Real.exp_add]; ring_nf
  nlinarith [Real.exp_one_gt_d9, Real.exp_one_lt_d9,
             Real.exp_pos (Real.exp (Real.log 3 + Real.log 3))]


end DivLowerBound3Full